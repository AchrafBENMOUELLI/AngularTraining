import { DatePersonPipe } from './date-person.pipe';

describe('DatePersonPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePersonPipe();
    expect(pipe).toBeTruthy();
  });
});
